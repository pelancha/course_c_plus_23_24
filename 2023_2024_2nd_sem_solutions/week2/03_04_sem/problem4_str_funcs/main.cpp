////////////////////////////////////////////////////////////////////////////////
/// \file
/// \brief      Main module for Problem 4: "Some functions for dealing with strings".
/// \author     Sergey Shershakov
/// \version    0.1.0
/// \date       19.01.2021
///             This code is for educational purposes of the course "Introduction
///             to programming" provided by the Faculty of Computer Science
///             at the Higher School of Economics.
///
/// Try to use functions for dealing with strings:
/// 1) strcmp, strlen for c-strings;
/// 2) string:: size(), length(), operator<, operator+, operator= etc for
///    std::string-s.
///
////////////////////////////////////////////////////////////////////////////////


#include <iostream>
#include <string>

int main()
{
    // TODO: input your code here
    // char a[] = 'qwerty';
    // char b[] = 'qwerty';
    // std::cout << strcmp(a, b) << '\n'; //выводит первый расходящийся символ и различие в лексиграфическом порядке

    // char a[] = "awerty";
    // char b[123] = 'awerty';

    // char c[sizeof(a) + sizeof(b)];
    // for (size_t i=0; i < sizeof(a); i++)
    //     c[i] = a[i];
    
    // for (size_t i=sizeof(a); i < sizeof(a) + sizeof(b); i++)
    //     c[i - 1] = b[i - sizeof(a)]; // -1 обрезает /0 показывающий конец массива а
    
    


    return 0;
}
